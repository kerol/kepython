sudo: false
language: python

python:
  - "2.7"
  - "3.6"

install:
    - pip install tox-travis

script:
    - tox

branches:
  only:
    - master
  except:
    - sub-branch

notifications:
  email: false
